import { AwesomeTooltipDirectiveDirective } from './awesome-tooltip-directive.directive';

describe('AwesomeTooltipDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AwesomeTooltipDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
