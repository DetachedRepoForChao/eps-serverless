import { PointItemPipe } from './point-item.pipe';

describe('PointItemPipe', () => {
  it('create an instance', () => {
    const pipe = new PointItemPipe();
    expect(pipe).toBeTruthy();
  });
});
